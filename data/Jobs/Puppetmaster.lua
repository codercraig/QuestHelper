-- =============================================================================
-- Puppetmaster job unlock
-- =============================================================================
-- SCAFFOLD ONLY - author the steps from wiki + server Lua, following
-- data/ref/step_authoring_guide.md. Use the new image format
-- (zone_name + state + highlights), never file = "...png".
--
-- Unlock requirement: Level 30; requires Treasures of Aht Urhgan
-- =============================================================================

return {
    ["Unlock Puppetmaster: TODO confirm quest name from wiki"] = {
        -- requirements = { level = 30 },
        steps = {
            {
                text = "TODO: author the Puppetmaster unlock quest steps.",
            },
        },
        -- reward = { text = "Unlocks the Puppetmaster job." },
    },
}
